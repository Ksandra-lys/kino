export type mostVotedFilms = {
    filmId : number,
    title: string,
    releaseYear : number,
    director:string,
    poster:string,
    tmdbId :number,
    depositedBy : string,
    voteCount : number
}